# Education

<iframe 
    src="https://snazzymaps.com/embed/568988" 
    width="100%" 
    height="400px" 
    style={{ border: '0' }}
></iframe>

import { Tabs } from 'nextra/components'
 
<Tabs items={['Programming Languages', 'Libraries', 'Applications']}>
  <Tabs.Tab>**Languages**: C++, Python, C, Matlab.</Tabs.Tab>
  <Tabs.Tab>**Libraries** Pytorch, Matplotlib, etc. </Tabs.Tab>
  <Tabs.Tab>**Applications** Revit, AutoCAD, Autodesk Inventor.</Tabs.Tab>
</Tabs>

## Component

import { useState } from 'react'

{/* Import CSS modules */}
import styles from '../components/counters.module.css'

export const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)} className={styles.counter}>Clicked {count} times</button>
    </div>
  );
};

<Counter/>

## External Component

import Counters from '../components/counters'

<Counters />
